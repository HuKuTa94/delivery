services:
  app:
    build:
      context: .
      dockerfile: docker/dev/app/Dockerfile.tasks
      args:
        - USER_ID=${USER_ID:-1000}
        - GROUP_ID=${GROUP_ID:-1000}
    ports:
      - "8080:8080"
